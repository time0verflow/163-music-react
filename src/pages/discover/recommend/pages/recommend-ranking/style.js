import styled from "styled-components";
import top from '../../../../../assets/img/recommend-top-bg.png'

export const RankingWrapper = styled.div`
  margin-bottom: 15px;
  .ranking-info {
    display: flex;
    margin-top: 17px;
    background: url(${top});
  }
`